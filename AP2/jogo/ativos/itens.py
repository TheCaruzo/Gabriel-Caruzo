from jogo.Personagens import Player
from Personagens.Player import hp


pocoes = {
    "pc" : int(hp * 0.20),
    "pc2" : int(hp * 0.40),
    "pf" : int(sta["fo"] + 1),
    "pf2" : int(status["fo"] + 2),
    "pd" : int(status["de"] + 1)
} 